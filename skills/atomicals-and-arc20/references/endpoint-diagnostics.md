# Endpoint diagnostics

Run in order: GET /live, GET /ready, GET /token-explorer/status. The lag is
chainTip minus atomicalsTip. A network failure is unreachable; never describe
it as deprecated or closed. The API Conformance Workbench
(/tools/api-conformance/) performs the contract checks and produces a
scrubbed report. Hosted surfaces must not proxy arbitrary endpoints.
