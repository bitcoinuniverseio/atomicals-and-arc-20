# Nested installs keep each workspace member's dependencies under its own
# node_modules. Astro hashes scoped styles by path, and only paths inside the
# site root are project-relative, so Starlight must resolve under site/ for the
# committed output to be reproducible anywhere.
install-strategy=nested
